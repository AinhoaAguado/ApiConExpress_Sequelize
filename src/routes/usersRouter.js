/*const express = require('express');
const router = express.Router();

const usersController = require('../controllers/usersController.js'); // Controladores: Define tus controladores en un archivo separado

// Rutas
router.get('/', usersController.getAllProducts);
router.get('/:id', usersController.getProductById);
router.post('/', usersController.createProduct);
router.put('/:id', usersController.updateProduct);
router.delete('/:id', usersController.deleteProduct);

module.exports = usersController;*/