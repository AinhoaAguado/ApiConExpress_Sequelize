/*const Category = require('../models/brandsModel.js'); // Importa el modelo User

// Ejemplo de cómo crear un nuevo usuario en la base de datos
Category.create({ category_name: 'zapatillas'})
  .then(category => {
    console.log('Usuario creado:', category.toJSON());
  })
  .catch(error => {
    console.error('Error al crear usuario:', error);
  });*/